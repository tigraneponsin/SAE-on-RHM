# Sparse Auto-encoders on the Random Hierarchy Model

Implement the Random Hierarchy Model (RHM) in PyTorch and train neural networks on the RHM dataset. The RHM is a synthetic multiclass classification dataset inspired by the hierarchical compositionality of natural data.

Use SAEs on transformers trained on classification of the RHM to try to uncover the latent variables. 
